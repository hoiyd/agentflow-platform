# Release Signing Guide

## Bundle validation

Run check `SIG-CHECK-11` to validate a release signing bundle. A passing result confirms that the manifest digest and artifact signatures agree with the trusted release key.
